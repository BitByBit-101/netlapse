backend/netlapse.db
backend/netlapse
# SQLite sidecar files, written whenever the DB is open in WAL mode
*.db-wal
*.db-shm
# Scratch databases from manual collector checks
backend/route-check.db
backend/*-check.db
frontend/node_modules
frontend/dist
# Linux binary from a cross-compile or a host deploy build
backend/netlapse-server
# Deployment bundle artifacts — rebuilt by deploy/build-bundle.sh on demand
deploy/netlapse-bundle.tar.gz
deploy/netlapse-bundle/
.env

# TypeScript incremental build cache — regenerated by `tsc -b`
frontend/tsconfig.tsbuildinfo
